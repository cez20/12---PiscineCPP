TODO:

[X] Implementez une classe Weapon(arme) qui possede:
	[X] Un attribut privé type de type string.
	[X] Une fonction membre getType() retournant une référence constante sur type.
	[X] Une fonction membre setType() qui attribue à type la nouvelle valeur passée en
		paramètre.

[] Creez deux classes: HumanA et HumanB
	HumanA et HumanB sont presques identiques a l'exception de quelques details
	[X] HumanA : Reference vers un Weapong 
		[X] Possedent un Weapon et un name; 
		[X] Le constructeur de HumanA prend une Weapon comme paramètre
		[X] fonction membre "attack()" qui affiche le message suivant
			<name> attack with their <weapon type>
		[X] HumanA aura forcement toujours une arme -- meme s'il s'agit d'une string vide ("")
	[X] HumanB : pointeur vers 
		[X] Possedent un Weapon et un name;
		[X] Creer une fonction setWeapon() qui set une arme pour HumanB; 
		[x] Le constructeur de HumanB ne prend PAS de Weapon comme parametre
		[X] fonction membre "attack()" qui affiche le message suivant
			<name> attack with their <weapon type>
		[X] HumanB n'aura PAS toujours une Weapon