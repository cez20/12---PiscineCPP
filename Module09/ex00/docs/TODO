*** Always do your project in the most straightforward way possible***

Premier jet: program fonctionnel end-to-end pour le cas le plus simple qui soit. 
- Je prends un fichier input.txt qui contient des dates et un nombre precis de bitcoin pour cette date precise
- Je prends le fichier la date de input.txt et je la cherche a l'interieur du fichier data.csv
- Si je trouve la date, je prends le nombre precise de bitcoin dans input.txt et je la multiples par le exchange rate trouve dans data.csv
- Je print les informations suivantes sur stdout
    - resultat final = [date database data.csv] => [nombre de bitcoin dans input.txt] = [result de nbr de bitcoin * exchange rate]
    - Ex: 2011-01-03 => 3 = 0.9

TODO:
[] Creer un version qui fonctionne de bout a bout:
    [X] Downloader le fichier data.csv et le mettre dans mon dossier de project
    [X] Creer un fichier input.txt et le remplir avec le contenu du PDF 
    [] Mettre le fichier data.csv dans un std::map
        [] Ouvrir le fichier data.csv
        [] Skipper la 1ere ligne
        [] Skip whitespace
        [] Mettre la data dans un variable std::string. Pour savoir la date, va jusqu'au prochain whitespace
        [] Skip whitespace
        [] Insert next element in unsigned integer
    [] git commit , git push
    [] Creer un fichier input.txt et le remplir avec les infos de l'exemple dans le PDF
    [] Looper dans le fichier input.txt
        [] Skipper la 1ere ligne
        [] Mettre dans une variable la date
        [] Mettre dans une variable pour le nbrOf Bitcoin
        [] std::map.find(date) = chercher la date du input.txt dans notre map
        [] S'il le trouve, imprimer contenu a l'ecran 

[] 